# Pipecat AI with required service integrations
pipecat-ai[assemblyai,openrouter,elevenlabs,mem0]

# Environment variable management
python-dotenv

# Enhanced logging
loguru 
pyyaml

# Local control plane (HTTP + WebSocket)
fastapi
uvicorn[standard]
 
# Twitch chat client (EventSub-based, v3)
twitchio>=3.1.0

# HTTP client for Helix user resolution
httpx